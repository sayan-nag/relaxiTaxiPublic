package com.relaxiTaxi.onboarding.service;

import org.springframework.stereotype.Component;

@Component
public class EmailVerificationService {

    //generate OTP

    //sent notification through notif service

    //persist against email in guava cache in this class

    // expire cache in 5 mins

    // verify method will validate entered OTP against cache
}
